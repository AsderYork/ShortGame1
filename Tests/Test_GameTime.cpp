#include "stdafx.h"
/*#define _SILENCE_CXX17_ITERATOR_BASE_CLASS_DEPRECATION_WARNING
#include <boost/test/unit_test.hpp>

#include <GameTime.h>

using namespace GEM::GameSim;

BOOST_AUTO_TEST_SUITE(GameTime, * boost::unit_test::tolerance(0.0001))
BOOST_AUTO_TEST_CASE(Comparison)
{
	float testvalue = 1.0f;
	for (int i = 0; i < 100; i++)
	{
		testvalue = static_cast<float>(rand()/(float)RAND_MAX);
		BOOST_TEST(GameTimeToSeconds(SecondsToGameTimeInterval(testvalue)) == testvalue);
	}
}


BOOST_AUTO_TEST_SUITE_END()*/